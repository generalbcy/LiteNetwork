namespace LiteNetwork.Protocol
{
    /// <summary>
    /// Provides an options that indicates the current packet mode.
    /// </summary>
    public enum LitePacketMode
    {
        /// <summary>
        /// Indicates that the packet is in Read-Only mode.
        /// </summary>
        Read,

        /// <summary>
        /// Indicates that the packet is in Write-Only mode.
        /// </summary>
        Write
    }
}
